- `soup data preprocess` now writes a `labels` column masked by the run's loss-mask mode, so a `pre_tokenized` run no longer silently trains on the prompt; the mask mode is part of the cache key, a cache without labels is refused, and `soup train` reports the real cached row count instead of `0` (#1054).
